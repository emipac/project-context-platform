[project]
name = "project-context-lightrag-service"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.8.0",
  "lightrag-hku[api]>=1.4.9"
]

[tool.uv]
package = false
